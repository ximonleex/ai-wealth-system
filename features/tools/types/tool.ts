export type ToolItem = {
  id: string;
  name: string;
  description: string;
  bestFor: string;
  keyBenefit: string;
  ctaText: string;
  affiliateUrl: string;
  iconLabel: string;
  trackingLabel?: string;
};
